{% extends 'base.html.twig' %}

{% block title %}Dossier de remboursement de {{ training_reimbursement.trainee }}{% endblock %}

{% block body %}
    <h1>Dossier de remboursement de {{ training_reimbursement.trainee }}</h1>

    {{ include('training_reimbursement/_show.html.twig') }}


    {% if is_granted('ROLE_ADMIN') %}
        <a href="{{ path('app_training_reimbursement_index') }}">retour à la liste</a>
        {{ include('training_reimbursement/_delete_form.html.twig') }}
    {% endif %}
{% endblock %}
